# What To Do

1. Create a New Log (Primary Focus)
- Creates a new log file and stores it in temp
- Uses a basic auto-generated layout (date, section headers, etc.)
- Displays the log in the terminal with line numbers
- Allows editing specific lines from the command line
- File-based only (clipboard support may be added later)

2. Review Notes (Read-Only, Simple)
- Reads notes from files in the notes directory
- Lists available note files or topics
- Displays selected content in the terminal
- Formatting to be improved later

3. Progress / Goal Tracker (Later)
- Creates and updates a permanent tracker file
- Tracks goals and basic progress information
- Implemented after core logging and note review work

## Features???

Include rich for colored output => like certain headers and stuff